require "spec_helper"

describe Sacrifice do
  it "has a version number" do
    expect(Sacrifice::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
